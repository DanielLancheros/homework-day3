function getQuantity(string){

    const totalQuantity = string.length;
    return totalQuantity;
}

const string = "🍌";
const stringQuantity = "The total quantity of " + string + " is " + getQuantity(string);

console.log(stringQuantity);

//Method 2
function getQuantity2 (parameterOne){
    const result = parameterOne;
}
