function stringLength (parameterOne) {

    const totalLength = parameterOne.length;
    return totalLength;
}

const parameterOne = "Daniel Lancheros";
const parameterLength = "The total characters of " + parameterOne + " is " + stringLength(parameterOne);

console.log(parameterLength);
